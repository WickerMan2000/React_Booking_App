import React from 'react';
import styles from './Property.module.css';

const Property = () => { 
    return (
        <div className={styles.Property}>Property type</div>
    );
}

export default Property;